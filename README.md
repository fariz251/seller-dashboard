# Seller Dashboard

Dashboard Shopee seller, React + Vite + Tailwind, data dari Supabase.

## Jalan lokal

```bash
npm install
cp .env.example .env.local
# edit .env.local, isi VITE_SUPABASE_URL dan VITE_SUPABASE_ANON_KEY
npm run dev
```

## Deploy ke Vercel

Lihat `DEPLOY.md`.
